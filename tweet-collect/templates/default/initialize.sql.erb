-- Database Initialized Configuration.

-- setup user information
set password for root@"localhost"=PASSWORD('<%= @mysql["root"]["password"] ? @mysql["root"]["password"] : "rootroot" %>');
grant all privileges on *.* to <%= @mysql["user"]["name"] ? @mysql["user"]["name"] ? "tweetuser" %>@'localhost' identified by '<%= @mysql["user"]["password"] ? @mysql["user"]["password"] ? "tweetuser_password" %>' with grant option;

-- create and setup database.
create database tweet_collect character set utf8;
use tweet_collect;
CREATE TABLE tweet (
    ID INTEGER NOT NULL AUTO_INCREMENT
  , MESSAGE VARCHAR(512)
  , CREATED VARCHAR(20)
  , PRIMARY KEY (ID)
);

